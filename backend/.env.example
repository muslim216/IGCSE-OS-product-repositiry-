# Copy to .env and fill in real values.

# Postgres (docker compose default). Tests override this with SQLite automatically.
DATABASE_URL=postgresql+asyncpg://igcse:igcse@localhost:5432/igcse

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=change-me-in-production

# Required for AI features (marking, extraction, chat, reports).
# The app runs without it; AI jobs fail gracefully with a clear error.
ANTHROPIC_API_KEY=

# Comma-separated allowed origins for the frontend.
CORS_ORIGINS=http://localhost:5173

# The refresh-token cookie is Secure by default (HTTPS only), which browsers
# drop on plain-HTTP local dev. Set to false for local http://localhost dev only.
REFRESH_COOKIE_SECURE=true

# Shadow-runs the Readiness Engine v2 job (compute_readiness_v2) alongside
# the live v1 engine on every evidence change, so v2 snapshots accumulate
# for comparison at GET /api/v1/readiness/v2/students/{id}. v1 keeps serving
# every existing endpoint either way — this does not change what the app
# shows. See CLAUDE.md's "Readiness v2" note.
READINESS_V2_SHADOW_ENABLED=false
