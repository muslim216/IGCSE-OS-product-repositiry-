dist
coverage
node_modules
package-lock.json

# Generated by the API type pipeline (task 0.8), not written by hand:
# openapi.json is dumped from the backend's own OpenAPI document and
# src/api/schema.d.ts is `npm run generate:api` over it. Formatting either one
# makes every regeneration a diff against Prettier rather than against the API,
# which is the drift these files exist to reveal. The generators' output is the
# committed form; tests/test_openapi_snapshot.py is what keeps it honest.
openapi.json
src/api/schema.d.ts
